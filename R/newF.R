
library(microbenchmark)
library(ptw)
library(compiler)

#'@export
newF <- function(n, d, l){
  count= 0
  for (i in 1:n){
    if (isTRUE(isEqualVecotrs())) {
      count= count+1
      #print(res)
    }
    E1 <- count*d/(n*l)


  }
  print(E1)
}
